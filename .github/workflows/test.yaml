name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps: [uses: answerdotai/workflows/nbdev3-ci@master]
      env:
        OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}